<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- BLE scan + connect. SCAN/CONNECT are the Android 12+ (API 31) model;
         neverForLocation says we don't derive location from scan results. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Legacy BLE permissions for API <= 30. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Location is for the map (geotagging fixed-device detections + the "you are
         here" dot), not the scan. Android 12+ silently ignores a FINE request that
         does not also ask for COARSE, so both must be declared. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- osmdroid tiles -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Haptic alerts for stealth mode. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Drive mode: a foreground service that posts the glanceable detection-counter
         notification and keeps the BLE link alive in the background. connectedDevice is the
         FGS type for a held GATT link; POST_NOTIFICATIONS is the Android 13+ runtime grant. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android 16 (API 36): post a promoted ongoing notification = the Live Update status-bar chip. -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Drive mode geotags hits from the phone's fix, so the service needs the location FGS type
         too: with connectedDevice alone the platform stops delivering fixes the moment the
         activity is no longer visible, and every hit for the rest of the drive gets stamped with
         the last coordinate we happened to see. The type is OR'd in AT RUNTIME and only when a
         location permission is actually held (see AcabLinkService.startForegroundCompat) - it is
         optional in this app, and passing a type the app has no permission for throws on 14+.
         Deliberately NOT ACCESS_BACKGROUND_LOCATION: the while-in-use FGS model covers a drive
         and avoids the Play Console background-location declaration. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- allowBackup=false: the detections file (MACs + capture GPS + timestamps) and the
         ignore-list prefs must not flow to cloud Auto-Backup or device-transfer. The
         Keystore-wrapped buffer-key blob is safe, but no app data should leave by backup. -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OuiAcab">
        <!-- singleTask: the drive-mode notification tap re-delivers its deep-link intent to
             the running activity (onNewIntent) instead of stacking a second instance.
             No screenOrientation lock: tablets/foldables get landscape and the two-pane layout.
             Rotation recreates the activity, which is lossless now that per-tab UI state rides
             rememberSaveable + ViewModels (tab shell SaveableStateHolder, ContributionViewModel,
             the Map camera saver); do not re-add the lock without re-checking those. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.OuiAcab">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Shares the exported detections CSV from cacheDir via a content:// uri. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Drive-mode foreground service: the glanceable detection-counter notification. -->
        <service
            android:name=".ble.AcabLinkService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location" />

        <!-- Nordic DFU library's worker service (updates the nRF co-processor over BLE). It runs
             as a connectedDevice foreground service for the ~20s of a transfer. -->
        <service
            android:name=".ble.NrfDfuService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Home-screen widget: today's detection count, last sighting, link state. The app
             writes a small summary to the "beacons_widget" shared-prefs file and pokes this
             receiver to re-render (the widget runs in the launcher process and can't read app
             memory). APPWIDGET_UPDATE is the system update broadcast. -->
        <receiver
            android:name=".widget.BeaconsWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_beacons_info" />
        </receiver>

        <!-- Quick Settings tile: toggles drive mode from the shade (the Control Center
             toggle's Android analog). BIND_QUICK_SETTINGS_TILE restricts binding to SystemUI. -->
        <service
            android:name=".svc.DriveModeTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_drive"
            android:label="Drive Mode"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>
</manifest>
